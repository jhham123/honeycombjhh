import experimentStart from "../trials/experimentStart";
import largeWhiteText from "../trials/largeWhiteText";

// default preamble 
const preamble = {
  type: "html_keyboard_response",
  stimulus: "",
  timeline: [experimentStart()]
};

export default preamble;
